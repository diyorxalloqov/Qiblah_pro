import 'package:flutter/cupertino.dart';

class Umumiy extends StatelessWidget {
  const Umumiy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('Umumiy'),
        )
      ],
    );
  }
}
