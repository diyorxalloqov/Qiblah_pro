import 'package:qiblah_pro/modules/global/imports/app_imports.dart';

class NamozPage extends StatelessWidget {
  const NamozPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text('NamozPage'),)
      ],
    );
  }
}