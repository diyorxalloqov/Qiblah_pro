enum LocationStatusEnum {
  notRequested,
  failed,
  deniedForever,
  denied,
  gpsDisabled,
  onlyApproximate,
  waiting,
  available
}

enum ActionStatus { isLoading, isSuccess, isError, isInitial }
